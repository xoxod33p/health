'use client';

import { Plus, ShieldCheck, UserRound } from 'lucide-react';
import { AppShell } from '../components/app-shell';

const members = [{ name: 'Olivia Chen', email: 'olivia.chen@northstarcare.org', role: 'Company admin', status: 'Active', initials: 'OC' }, { name: 'Aisha Morgan', email: 'aisha.morgan@northstarcare.org', role: 'Manager', status: 'Active', initials: 'AM' }, { name: 'Jordan Tate', email: 'jordan.tate@northstarcare.org', role: 'Healthcare employee', status: 'Active', initials: 'JT' }, { name: 'Ravi Khatri', email: 'ravi.khatri@northstarcare.org', role: 'Auditor', status: 'Invited', initials: 'RK' }];

export default function TeamPage() { return <AppShell><div className="page-content"><section className="page-heading"><div><p className="eyebrow">People & access</p><h1>Team</h1><p className="heading-copy">Manage workspace members and their permissions.</p></div><button className="primary-button"><Plus size={17} /> Invite member</button></section><section className="role-banner"><ShieldCheck size={19} /><div><strong>Role-based access is enabled</strong><span>Permissions are enforced by the API for every request.</span></div><button className="ghost-button">Review roles →</button></section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Northstar Care</p><h2>Workspace members</h2></div><span className="result-count">{members.length} members</span></div><div className="member-list">{members.map((member) => <div className="member-row" key={member.email}><div className="member-avatar">{member.initials}</div><div className="member-copy"><strong>{member.name}</strong><span>{member.email}</span></div><span className="role-label">{member.role}</span><span className={`status status-${member.status.toLowerCase()}`}><i />{member.status}</span><button className="icon-button" aria-label={`Manage ${member.name}`}><UserRound size={17} /></button></div>)}</div></section></div></AppShell>; }
